@page "/importacao/consulta"
@page "/importacao/consulta/{Codigo:int}"

@using MudBlazor.Utilities

<p>Código: @Codigo</p>
<p>Código: @Codigo2</p>

<MudGrid>
    <MudItem md="12"
             sm="12"
             xs="12">
        <MudBreadcrumbs Separator=">"
                        Items="_items"
                        Style="font-size: 10px;"
                        Class="relative d-flex justify-end" />
    </MudItem>
</MudGrid>

<br />
<MudGrid>
    <MudItem md="12"
             sm="12"
             xs="12">
        @if (ListaNotasFiscais is not null)
        {
            <MudTable Items="@ListaNotasFiscais" Hover="true" Breakpoint="Breakpoint.Sm">
                <HeaderContent>
                    <MudTh>nNF</MudTh>
                    <MudTh>chNFe</MudTh>
                    <MudTh>Emitente</MudTh>
                    <MudTh>Destinatário</MudTh>
                    <MudTh></MudTh>
                    <MudTh></MudTh>
                </HeaderContent>
                <RowTemplate>
                    <MudTd DataLabel="nNF">@context.nNF</MudTd>
                        <MudTd DataLabel="chNFe">@context.chNFe</MudTd>
                        <MudTd DataLabel="Emitente">@context.xNomeEmit</MudTd>
                        <MudTd DataLabel="Destinatário">@context.xNomeDest</MudTd>
                        <!-- Botões Editar/Deletar -->
                        <MudTd DataLabel="Editar">
                            <MudIconButton Icon="@Icons.Material.Filled.Edit" Color="Color.Primary" aria-label="delete"/>
                        </MudTd>
                        <MudTd DataLabel="Deletar">
                            <MudIconButton OnClick="@(() => DeletarNotaFiscal(context.IdNotaFiscal))" Icon="@Icons.Material.Filled.Delete" Color="Color.Error" aria-label="delete"/>
                        </MudTd>

                    </RowTemplate>
                </MudTable>
        }
    </MudItem>
</MudGrid>

@if (!Equals(_TESTE,null))
{
    <p>Nome: @_TESTE.Nome</p>
    <p>Nome2: @_TESTE.Nome2</p>
    
}